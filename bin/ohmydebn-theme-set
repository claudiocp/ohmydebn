#!/bin/bash

# ohmydebn-theme-set: Set a theme, specified by its name.
# Usage: ohmydebn-theme-set <theme-name>

if [[ -z "$1" && "$1" != "CNCLD" ]]; then
  echo "Usage: ohmydebn-theme-set <theme-name>" >&2
  exit 1
fi

THEMES_DIR="$HOME/.config/ohmydebn/themes/"
CURRENT_THEME_DIR="$HOME/.config/ohmydebn/current/theme"

THEME_NAME=$(echo "$1" | sed -E 's/<[^>]+>//g' | tr '[:upper:]' '[:lower:]' | tr ' ' '-')
THEME_PATH="$THEMES_DIR/$THEME_NAME"

# Check if the theme entered exists
if [[ ! -d "$THEME_PATH" ]]; then
  echo "Theme '$THEME_NAME' does not exist in $THEMES_DIR" >&2
  exit 2
fi

# Update theme symlinks
ln -nsf "$THEME_PATH" "$CURRENT_THEME_DIR"

# Determine light or dark mode
if [ -f ~/.config/ohmydebn/current/theme/light.mode ]; then
  gsettings set org.x.apps.portal color-scheme "prefer-light"
else
  gsettings set org.x.apps.portal color-scheme "prefer-dark"
fi

# Define ROFI_DIR
ROFI_DIR=~/.config/rofi

# Define default themes for gtk, icon, cinnamon, and rofi
GTK_THEME="Mint-Y-Dark-Blue"
ICON_THEME="Mint-Y-Blue"
CINNAMON_THEME="Mint-Y-Dark-Blue"
ROFI_THEME="rounded-white.rasi"

# Override those settings for our default OhMyDebn/Omarchy themes
case $THEME_NAME in
catppuccin)
  GTK_THEME="Catppuccin-Dark"
  ICON_THEME="Catppuccin-Mocha"
  CINNAMON_THEME="Catppuccin-Dark"
  ROFI_THEME="$ROFI_DIR/rounded-purple-dark.rasi"
  ;;
catppuccin-latte)
  GTK_THEME="Catppuccin-Light"
  ICON_THEME="Catppuccin-Latte"
  CINNAMON_THEME="Catppuccin-Light"
  ROFI_THEME="$ROFI_DIR/rounded-pink-dark.rasi"
  ;;
everforest)
  GTK_THEME="Everforest-Dark"
  ICON_THEME="Everforest-Dark"
  CINNAMON_THEME="Everforest-Dark"
  ROFI_THEME="$ROFI_DIR/rounded-green-dark.rasi"
  ;;
gruvbox)
  GTK_THEME="Gruvbox-Dark"
  ICON_THEME="gruvbox_dark"
  CINNAMON_THEME="Gruvbox-Dark"
  ROFI_THEME="$ROFI_DIR/rounded-green-dark.rasi"
  ;;
kanagawa)
  GTK_THEME="Kanagawa-Dark"
  ICON_THEME="Kanagawa"
  CINNAMON_THEME="Kanagawa-Dark"
  ROFI_THEME="$ROFI_DIR/rounded-blue-dark.rasi"
  ;;
matte-black)
  GTK_THEME="Gruvbox-Yellow-Dark"
  ICON_THEME="Mint-Y-Sand"
  CINNAMON_THEME="Gruvbox-Yellow-Dark"
  ROFI_THEME="$ROFI_DIR/rounded-red-dark.rasi"
  ;;
nord)
  GTK_THEME="Catppuccin-Dark-Macchiato"
  ICON_THEME="Catppuccin-Macchiato"
  CINNAMON_THEME="Catppuccin-Dark-Macchiato"
  ROFI_THEME="$ROFI_DIR/rounded-blue-dark.rasi"
  ;;
ohmydebn)
  GTK_THEME="Catppuccin-Dark"
  ICON_THEME="Catppuccin-Mocha-Alt2"
  CINNAMON_THEME="Catppuccin-Dark"
  ROFI_THEME="$ROFI_DIR/rounded-blue-dark.rasi"
  ;;
osaka-jade)
  GTK_THEME="Osaka-Teal-Dark"
  ICON_THEME="Osaka_Dark"
  CINNAMON_THEME="Osaka-Teal-Dark"
  ROFI_THEME="$ROFI_DIR/rounded-green-dark.rasi"
  ;;
ristretto)
  GTK_THEME="Gruvbox-Dark"
  ICON_THEME="Mint-Y-Sand"
  CINNAMON_THEME="Gruvbox-Dark"
  ROFI_THEME="$ROFI_DIR/rounded-orange-dark.rasi"
  ;;
rose-pine)
  GTK_THEME="Rosepine-Light"
  ICON_THEME="Rose-Pine"
  CINNAMON_THEME="Rosepine-Light"
  ROFI_THEME="$ROFI_DIR/rounded-pink-dark.rasi"
  ;;
tokyo-night)
  GTK_THEME="Tokyonight-Dark"
  ICON_THEME="Tokyonight-Dark"
  CINNAMON_THEME="Tokyonight-Dark"
  ROFI_THEME="$ROFI_DIR/rounded-purple-dark.rasi"
  ;;
*)
  # If the user has installed extra themes, then check to see if the theme specifies an icon theme
  # If so, then use the the icon theme to pick an appropriate color palette
  if [[ -f ~/.config/ohmydebn/current/theme/icons.theme ]]; then
    # check to see if there is an equivalent mint theme
    THEME=$(cat ~/.config/ohmydebn/current/theme/icons.theme)
    GENERIC_THEME=$(echo $THEME | sed s'|Yaru-||g')
    GENERIC_THEME_CASED=${GENERIC_THEME^}
    MINT_THEME="Mint-Y-$GENERIC_THEME_CASED"
    if [ -d /usr/share/icons/$MINT_THEME ] &&
      [ -d /usr/share/themes/$MINT_THEME ] &&
      [ -f $ROFI_DIR/rounded-$GENERIC_THEME-dark.rasi ]; then
      # GENERIC_THEME = purple, blue, red
      ICON_THEME="$MINT_THEME"
      ROFI_THEME="$ROFI_DIR/rounded-$GENERIC_THEME-dark.rasi"
      if [ -f ~/.config/ohmydebn/current/theme/light.mode ]; then
        GTK_THEME="$MINT_THEME"
        CINNAMON_THEME="$MINT_THEME"
      else
        GTK_THEME="Mint-Y-Dark-$GENERIC_THEME_CASED"
        CINNAMON_THEME="Mint-Y-Dark-$GENERIC_THEME_CASED"
      fi
    else
      # if we can't automatically convert to a mint theme, then try a few static mappings
      case $THEME in
      Yaru-sage | Yaru-olive)
        ICON_THEME="Mint-Y"
        ROFI_THEME="$ROFI_DIR/rounded-green-dark.rasi"
        if [ -f ~/.config/ohmydebn/current/theme/light.mode ]; then
          GTK_THEME="Mint-Y"
          CINNAMON_THEME="Mint-Y"
        else
          GTK_THEME="Mint-Y-Dark"
          CINNAMON_THEME="Mint-Y-Dark"
        fi
        ;;
      Yaru-yellow)
        ICON_THEME="Mint-Y-Sand"
        ROFI_THEME="$ROFI_DIR/rounded-yellow-dark.rasi"
        if [ -f ~/.config/ohmydebn/current/theme/light.mode ]; then
          GTK_THEME="Mint-Y-Sand"
          CINNAMON_THEME="Mint-Y-Sand"
        else
          GTK_THEME="Mint-Y-Dark-Sand"
          CINNAMON_THEME="Mint-Y-Dark-Sand"
        fi
        ;;
      Yaru-magenta)
        ICON_THEME="Mint-Y-Purple"
        ROFI_THEME="$ROFI_DIR/rounded-purple-dark.rasi"
        if [ -f ~/.config/ohmydebn/current/theme/light.mode ]; then
          GTK_THEME="Mint-Y-Purple"
          CINNAMON_THEME="Mint-Y-Purple"
        else
          GTK_THEME="Mint-Y-Dark-Purple"
          CINNAMON_THEME="Mint-Y-Dark-Purple"
        fi
        ;;
      esac
    fi
  fi
  ;;
esac

# use gsettings to update gtk theme, icon theme, and cinnamon theme
gsettings set org.cinnamon.desktop.interface gtk-theme "$GTK_THEME"
gsettings set org.cinnamon.desktop.interface icon-theme "$ICON_THEME"
gsettings set org.cinnamon.theme name "$CINNAMON_THEME"

# update rofi theme
mkdir -p $ROFI_DIR
cat <<EOF >$ROFI_DIR/config.rasi
@theme "$ROFI_THEME"
EOF

# Trigger alacritty config reload
ALACRITTY_CONFIG_DIR=~/.config/alacritty
mkdir -p $ALACRITTY_CONFIG_DIR
touch $ALACRITTY_CONFIG_DIR/alacritty.toml

# Restart components to apply new theme
pkill -SIGUSR2 btop

# Update starship prompt
if [ "$1" == "Ohmydebn" ]; then
  cp ~/.local/share/ohmydebn/config/starship.toml ~/.config/starship.toml
else
  cp ~/.local/share/omarchy/config/starship.toml ~/.config/starship.toml
fi

# Set new background
~/.local/share/ohmydebn/bin/ohmydebn-theme-bg-next
